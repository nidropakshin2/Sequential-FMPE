prior:
    gamma_range: [0.05, 0.5] # tuple, default=(0.05, 0.5)
    beta_range: [0.5, 5.0] # tuple, default=(0.5, 5.0)
simulator:
    S: [1000, 2000] # int, tuple, number of Susceptible individuals or bounds for following uniform distribution
    I: [100, 300] # int, tuple, number of Infectious individuals or bounds for following uniform distribution
    R: [0, 100] # int, tuple, number of Recovered individuals or bounds for following uniform distribution
    T: [60, 120] # int, tuple, simulation duration in days or bounds for following uniform distribution 
summary: "handmade"